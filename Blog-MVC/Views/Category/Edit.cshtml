@model Blog_MVC.Models.Category

@{
    ViewBag.Title = "Edit";
}

<div class="container">
    <div class="well">
        <h1><b>Редактиране на категория</b></h1>
        @using (Html.BeginForm("Edit", "Category", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            <div class="form-group">
                @Html.LabelFor(m => m.Name,"Име на категория", new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-4 col-sm-offset-4">
                    @Html.ActionLink("Отказ", "List", "User", new { @class = "btn btn-default" })
                    <input type="submit" value="Редакция" class="btn btn-success" />
                </div>
            </div>
        }
    </div>
</div>
<hr />
