@model Blog_MVC.Models.EditUserViewModel

@{
    ViewBag.Title = "Edit";
}

<div class="container">
    <div class="well">
        <h1><b>Редакция на потребител</b></h1>
        @Html.ValidationSummary()
        @Html.AntiForgeryToken()

        @using (Html.BeginForm("Edit", "User", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            <div class="form-group">
                @Html.LabelFor(m => m.User.Email,"Електронна Поща", new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextBoxFor(m => m.User.Email, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.User.FullName, "Пълно име", new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextBoxFor(m => m.User.FullName, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.Password, "Парола", new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextBoxFor(m => m.Password, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(m => m.ConfirmPassword, "Потвърждение на парола", new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextBoxFor(m => m.ConfirmPassword, new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                @for (int i = 0; i < Model.Roles.Count; i++)
                {
                    <div class="col-sm-offset-4 col-sm-4">
                        @Html.HiddenFor(m => m.Roles[i].Name)
                        @Html.CheckBoxFor(m => m.Roles[i].IsSelected)
                        @Model.Roles[i].Name
                    </div>
                }
            </div>

            <div class="form-group">
                <div class="col-sm-4 col-sm-offset-4">
                    <input type="submit" value="Редакция" class="btn btn-success" />
                </div>
            </div>
        }
    </div>
</div>
